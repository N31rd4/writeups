pas de subdomains
path /backups dispo (et uploads ?)
/uploads(+nimporte quoi)
et admin.php
RFI ? pas reussi a upload de .php
LFI ? la path semble pas sensible a ce qu'il y a avant /
on peut acceder aux fichiers des autres avec username + nom du fichier (http://nocturnal.htb/view.php?username={username}&file={filename})
admin semble etre tjrs setup
pas de mdp bruteforce
j'arrive pas a acceder a un fichier
je me rends compte que les fichiers des autres utilisateurs sont listés directement (img: ./screenshots/otheruser.jpg)
il faut donc surement juste trouver le bon user, j'utilise une wordlist et trouve 4 users (img: /screenshots/4users.jpg)
le seul fichier accessible est "privacy.odt" de amanda et dedans je trouves un mdp (img: ./screenshots/privacy.jpg)
on a donc amanda:arHkG7HAI68X8s1J
ssh fonctionne pas mais je peux me connecter a son compte
je vois qu'elle a acces au pannel d'administration (img: ./screenshots/admin.jpg)
je vois que je peux créer des backups qui sont stockés dans cette path /backups/backup_2025-07-04.zip
j'essaye de voir si je peux récuperer d'autres backups maintenant que j'ai le format (script: ./scripts/download.py)
j'en trouves aucun
dans les fichiers listés il y a précisé "File Structure (PHP Files Only)", je me dis que c'est un indice pour chercher des fichiers non PHP
il y a un bouton pour créer un backup, le mdp est enregistré dans cette variable $password = cleanEntry($_POST['password']);
qui fait partie d'une commande executée sur le serveur $command = "zip -x './backups/*' -r -P " . $password . " " . $backupFile . " .  > " . $logFile . " 2>&1 &";
je peux donc surement hijack la commande pour acceder au serveur avec un reverse shell
le problème étant que le password passe avant par une blacklist
function cleanEntry($entry) {
    $blacklist_chars = [';', '&', '|', '$', ' ', '`', '{', '}', '&&'];

    foreach ($blacklist_chars as $char) {
        if (strpos($entry, $char) !== false) {
            return false; // Malicious input detected
        }
    }

    return htmlspecialchars($entry, ENT_QUOTES, 'UTF-8');
}
je peux utiliser le retour a la ligne pour insérer mes commandes et le tab pour remplacer les espaces
mais j'arrives pas a faire un reverse shell avec ça
je me dis que je peux upload un reverse shell avec une extension autorisée puis ensuite le bouger dans une path ou il sera interprété par le serveur en modifiant l'extension en .php
(img: ./screenshots/upload.jpg)
je localise le fichier a modifier
(img: ./screenshots/location.jpg)
je le modifie l'extension en .php
(img: ./screenshots/bypassrestrictions.jpg)
Ça fonctionne !
(img: ./screenshots/foothold.jpg)
En arrivant je me souviens dans les fichiers php qu'il y avait une database
je vais sur dans le dossier du site et je la télécharge en espérant trouver des mdp (img: screenshots/database.jpg)
je trouves effectivement des hashs de mdp (img: ./screenshots/db_entries.jpg)
et en allant sur crackstation je trouve le mdp de l'utilisateur tobias (img: ./screenshots/tobias.jpg)
tobias:slowmotionapocalypse
Le user flag est dans son home

je remarque un serveur qui tourne en interne sur le port 8080 lancé par le root
dessus tourne ISPconfig, un panel d'administration pour les serveurs qui possede un CVE (src: https://seclists.org/fulldisclosure/2023/Dec/2)
qui me permetterais de faire du RCE
une path admin/language_edit.php qui permet ensuite de génerer dinamiquement du php qui sera executé est mal sanitizée ce qui permet d'executer du code arbitraire
un exploit existe (url: https://github.com/ajdumanhug/CVE-2023-46818)
je fais passer le port interne en externe avec un ssh tunnel
j'utilise l'exploit et BINGO je me retrouve avec un shell en root
Le flag final est dans /root/root.txt